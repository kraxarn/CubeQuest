<PreferenceScreen
	xmlns:app="http://schemas.android.com/apk/res-auto">
	
	<PreferenceCategory
		app:title="Appearance">

		<ListPreference
			app:key="map_theme"
			app:title="Map Theme"
			app:summary="Theme of the map, currently light"
			app:entries="@array/map_theme_entries"
			app:entryValues="@array/map_theme_values"/>

	    <SwitchPreferenceCompat
	      app:key="fullscreen"
	      app:title="Fullscreen"
	      app:summary="Hide the status and navigation bar (if any)"/>

	</PreferenceCategory>

	<PreferenceCategory
		app:title="Battery Saver">

		<SwitchPreferenceCompat
			app:key="black_map"
			app:title="Black Map"
			app:summary="Use black colors on the map"/>

		<SwitchPreferenceCompat
			app:key="black_app"
			app:title="Black App"
			app:summary="Use black colors instead of brown in the app"/>

		<SwitchPreferenceCompat
			app:key="background_updates"
			app:title="Background Updates"
			app:summary="Update location in the background with lowered accuracy"/>

		<SwitchPreferenceCompat
			app:key="location_accuracy"
			app:title="Lower location accuracy"
			app:summary="Use WiFi and cell towers instead of GPS to get device location"/>

	</PreferenceCategory>

	<PreferenceCategory
		app:title="Audio">

		<SeekBarPreference
			app:key="music_volume"
			app:title="Music Volume"/>

		<SeekBarPreference
			app:key="sound_volume"
			app:title="Sound Effects Volume"/>

	</PreferenceCategory>

	<PreferenceCategory
		app:title="About">

		<Preference
			app:key="credits"
			app:title="Credits"
			app:summary="The developers who worked on it"/>

		<Preference
			app:key="licenses"
			app:title="Licenses"
			app:summary="Open source licenses used to make the app"/>

		<Preference
			app:key="version"
			app:title="Version"
			app:summary="v1.0-development"/>

		<Preference
			app:key="debug_preferences"
			app:title="Show preferences"
			app:summary="DEBUG: Show current shared preferences"/>

	</PreferenceCategory>

	<PreferenceCategory
		app:title="Account">

		<Preference
			app:key="log_out"
			app:title="Log out"
			app:summary="Log out and close the app"/>
		
		<Preference
			app:key="reset_progress"
			app:title="Reset progress"
			app:summary="Delete your save and create a new one"/>

		<Preference
			app:key="view_progress"
			app:title="View progress"
			app:summary="Show the cloud save UI"/>

		<Preference
			app:key="load_progress"
			app:title="Load progress"
			app:summary="Show data for your most recent save"/>

		<Preference
			app:key="save_progress"
			app:title="Save progress"
			app:summary="Save your progress to the cloud"/>

	</PreferenceCategory>
</PreferenceScreen>